http:
  middlewares:
    simpleAuth:
      basicAuth:
        users:
          - '$secret'
  routers:
    api:
      rule: Host(`traefik.$domain`)
      entrypoints:
        - websecure
      middlewares:
        - simpleAuth
      service: api@internal
      tls:
        certResolver: lets-encrypt

